{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently \"npm run server\" \"npm run test:unit\"",
    "server": "nodemon backend/server.js",
    "build": "npm install --legacy-peer-deps && npm install --legacy-peer-deps --prefix frontend && npm run build --prefix frontend",
    "test:unit": "jest --config ./backend/jest.config.js --runInBand",
    "test": "npm --prefix backend test"
  },
  "keywords": [
    "mern",
    "chat-app"
  ],
  "author": "Nisa",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "colors": "^1.4.0",
    "dotenv": "^9.0.2",
    "express": "^4.17.1",
    "express-async-handler": "^1.1.4",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.12.9",
    "newman": "^6.2.1",
    "nodemon": "^2.0.7",
    "socket.io": "^4.1.2"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "concurrently": "^9.2.0",
    "cross-env": "^7.0.3",
    "jest": "^30.0.2",
    "selenium-webdriver": "^4.33.0",
    "supertest": "^7.1.1"
  }
}
